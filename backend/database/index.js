const {
  pool,
  query,
  testConnection,
  ensureSchema,
  ensureRankingsWebsiteSchema,
} = require('./connection');

module.exports = {
  pool,
  query,
  testConnection,
  ensureSchema,
  ensureRankingsWebsiteSchema,
};
